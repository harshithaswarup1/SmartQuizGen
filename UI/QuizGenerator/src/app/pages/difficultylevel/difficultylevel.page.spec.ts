import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DifficultylevelPage } from './difficultylevel.page';

describe('DifficultylevelPage', () => {
  let component: DifficultylevelPage;
  let fixture: ComponentFixture<DifficultylevelPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DifficultylevelPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
